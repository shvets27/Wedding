# Wedding Website - THE SHVETS FAMILY

Элегантный сайт-приглашение на свадьбу NIKITA & ALINA, созданный на Vue 3 с Vite.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Сайт будет доступен по адресу: **http://localhost:3000**

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Предпросмотр продакшен-сборки

```bash
npm run preview
```

## 📁 Структура проекта

```
wedding/
├── src/
│   ├── components/          # Vue компоненты
│   │   ├── HeroSection.vue      # Главная секция
│   │   ├── AboutSection.vue     # Наша история + музыкальный проигрыватель
│   │   ├── LogoSection.vue      # Логотип
│   │   ├── PhotoCollageSection.vue  # Галерея фотографий
│   │   ├── DetailsSection.vue   # Детали свадьбы
│   │   ├── ProgramSection.vue   # Программа дня
│   │   ├── RSVPSection.vue      # Анкета гостя
│   │   ├── ContactSection.vue    # Контакты
│   │   └── FooterSection.vue    # Футер
│   ├── composables/         # Композаблы Vue
│   │   ├── useSmoothScroll.js
│   │   ├── useScrollAnimations.js
│   │   └── useRSVP.js
│   ├── App.vue              # Главный компонент
│   ├── main.js              # Точка входа
│   └── styles.css           # Глобальные стили
├── index.html               # HTML шаблон
├── package.json             # Зависимости
├── vite.config.js          # Конфигурация Vite
└── README.md               # Этот файл
```

## 🌐 Публикация на GitHub Pages

### Шаг 1: Подготовка проекта

Перед загрузкой на GitHub убедитесь, что проект собран и готов:

```bash
# Установите зависимости
npm install

# Проверьте, что проект работает
npm run dev

# Соберите для продакшена
npm run build
```

### Шаг 2: Создайте репозиторий на GitHub

1. Перейдите на [GitHub](https://github.com)
2. Нажмите **New repository** (или **+** → **New repository**)
3. Введите название репозитория (например, `wedding-website`)
4. Выберите **Public** (для бесплатного GitHub Pages)
5. **НЕ добавляйте** README, .gitignore или лицензию (они уже есть в проекте)
6. Нажмите **Create repository**

### Шаг 3: Инициализируйте Git и загрузите проект

Откройте терминал в папке проекта и выполните:

```bash
# Инициализация Git
git init

# Добавление всех файлов (кроме тех, что в .gitignore)
git add .

# Первый коммит
git commit -m "Initial commit: Wedding website - THE SHVETS FAMILY"

# Добавление удаленного репозитория
# ЗАМЕНИТЕ YOUR_USERNAME и REPO_NAME на ваши данные!
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Переименование ветки в main
git branch -M main

# Загрузка на GitHub
git push -u origin main
```

**Важно:** Замените `YOUR_USERNAME` на ваш GitHub username и `REPO_NAME` на название репозитория.

### Шаг 4: Настройка GitHub Pages (автоматический деплой)

#### Вариант А: Автоматический деплой через GitHub Actions (РЕКОМЕНДУЕТСЯ)

Проект уже содержит файл `.github/workflows/deploy.yml` для автоматического деплоя.

1. После загрузки кода на GitHub перейдите в **Settings** вашего репозитория
2. В левом меню выберите **Pages**
3. В разделе **Build and deployment**:
   - **Source**: выберите **GitHub Actions**
4. Сохраните настройки

После каждого push в ветку `main` сайт будет автоматически собираться и публиковаться!

#### Вариант Б: Ручная настройка GitHub Pages

1. Перейдите в **Settings** вашего репозитория
2. В левом меню выберите **Pages**
3. В разделе **Build and deployment**:
   - **Source**: выберите **Deploy from a branch**
   - **Branch**: выберите `main` и папку `/ (root)`
4. Нажмите **Save**

### Шаг 5: Доступ к сайту

После настройки GitHub Pages ваш сайт будет доступен по адресу:
```
https://YOUR_USERNAME.github.io/REPO_NAME/
```

Например: `https://nikita.github.io/wedding-website/`

**Время публикации:** Обычно 1-2 минуты после push.

### Обновление сайта

После любых изменений в коде:

```bash
# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Загрузите на GitHub
git push
```

GitHub Actions автоматически соберет и опубликует обновленную версию сайта.

## ✏️ Настройка контента

### Изменить имена

**HeroSection.vue:**
```vue
const heroTitle = ref('NIKITA & ALINA')
```

### Изменить дату и место

**DetailsSection.vue:**
```js
const details = ref([
  {
    title: 'ДАТА',
    main: '10 Сентября 2026',
    time: '12:00',
    type: 'date'
  },
  {
    title: 'МЕСТО',
    main: 'Усадьба Марьино',
    time: 'Адрес места проведения',
    type: 'location',
    mapLink: 'https://yandex.ru/maps/-/CLDPUD4G'
  }
])
```

### Изменить контакты

**ContactSection.vue:**
```js
const contacts = ref([
  {
    id: 1,
    role: 'Жених',
    name: 'Никита',
    phone: '+7 (981) 840-44-94'
  },
  {
    id: 2,
    role: 'Невеста',
    name: 'Алина',
    phone: '+7 (981) 827-96-13'
  }
])
```

### Изменить программу дня

**ProgramSection.vue:**
```js
const timeline = ref([
  {
    time: '15:00',
    title: 'Церемония',
    description: 'Торжественная регистрация брака'
  }
])
```

### Изменить фотографии

**PhotoCollageSection.vue:**
```js
const photos = ref([
  { src: 'https://i6.imageban.ru/out/2025/12/22/...', alt: 'Фото 1' }
])
```

## 🎵 Музыкальный проигрыватель

Сайт включает музыкальный проигрыватель с интеграцией VK Playlist:
- Кнопка Play/Pause в секции "Наша история"
- VK Playlist Widget (появляется на 1 секунду, затем скрывается)
- Автоматическое воспроизведение при нажатии

**Настройка VK Playlist:**
В `AboutSection.vue` найдите:
```js
window.VK.Widgets.Playlist("vk_playlist_145834956_83", 145834956, 83, '650c111c934db31211')
```

## 📱 Особенности

- ✅ Полностью адаптивный дизайн
- ✅ Плавные анимации при прокрутке
- ✅ Музыкальный проигрыватель (VK Playlist)
- ✅ Галерея фотографий с Lightbox
- ✅ Анкета гостя с отправкой данных
- ✅ Интеграция с календарем (Google Calendar)
- ✅ Ссылки на Яндекс.Карты
- ✅ Элегантный минималистичный дизайн

## 🛠️ Технологии

- **Vue 3** - Progressive JavaScript Framework
- **Vite** - Next Generation Frontend Tooling
- **Composition API** - для организации логики компонентов

## 📝 Лицензия

Создано с любовью для NIKITA & ALINA

---

**THE SHVETS FAMILY**
